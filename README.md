# latch

`latch` is a project-scoped coordination ledger for AI agents sharing a
workspace. It persists the facts that should survive chat history loss:
path claims, decisions, handoff tasks, contracts, and repo hazards.

The current design draft is in [docs/SPEC.md](docs/SPEC.md).

